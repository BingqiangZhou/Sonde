# PodcastInsight v3 环境变量示例
# v3 是纯静态站 + 本地 skill 脚本，通常无需任何环境变量即可运行。

# 可选：覆盖数据目录位置（默认为仓库根的 data/）
# 用于测试或 CI 中将数据指向其他路径
# PODCASTINSIGHT_DATA_DIR=/path/to/data

# 可选：覆盖 LLM 配置（仅当通过非 agent 方式调用 summarize 时需要，
# 标准用法是 agent 在对话中直接生成摘要，不需要这些变量）
# LLM_API_KEY=sk-xxxx
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini
