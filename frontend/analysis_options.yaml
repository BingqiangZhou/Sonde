# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
  errors:
    # Treat missing required parameters as errors
    missing_required_param: error
    # Treat missing return statements as errors
    missing_return: error

linter:
  rules:
    avoid_print: true
    cancel_subscriptions: true
    close_sinks: true
    # Disable noisy rules that are overly strict for this project
    public_member_api_docs: false
    prefer_single_quotes: false
    lines_longer_than_80_chars: false
    one_member_abstracts: false
    # 服务/UI 层大量使用刻意宽泛的 catch 做容错（记录日志后降级），
    # 加 on 子句会改变错误传播语义
    avoid_catches_without_on_clauses: false
    # 音频/UI 层的 fire-and-forget 调用是有意为之，逐个包 unawaited() 只产生噪音
    discarded_futures: false
    unawaited_futures: false
    # 纯风格规则，修复需要大范围改动已验证的代码，收益不成比例
    cascade_invocations: false
